name: bible_samples
description: Executable samples of the Dart/Flutter bible doctrine.
publish_to: none
environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  equatable: ^2.0.0
  fpdart: ^1.2.0

dev_dependencies:
  lints: ^5.0.0
  shouldly: ^0.5.0+1
  test: ^1.25.0
